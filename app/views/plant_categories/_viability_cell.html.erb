<td id="plant_category_<%= category.id %>_viability" class="px-6 py-4 text-sm text-gray-500">
  <% if category.expected_viability_years.present? %>
    <%= category.expected_viability_years %>
    <% if category.expected_viability_years_ai_populated? %>
      <span class="inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-blue-50 text-blue-700 ml-1" title="AI-suggested — you can override this value anytime">AI</span>
    <% end %>
  <% else %>
    <span class="text-gray-400">—</span>
  <% end %>
</td>
